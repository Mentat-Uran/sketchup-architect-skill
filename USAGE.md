# SketchUp Architect 使用规范

这份文档是面向用户和智能体的工作约定。它补充 `SKILL.md` 的执行规则，重点记录什么时候使用这个 skill、哪些证据可以声称、哪些结果必须标记为未验证，以及建筑方案和 SketchUp 文件应如何交付。

用户当前任务的明确要求优先于本规范：如果用户要求仅分析、离线、禁止联网、禁止启动 SketchUp 或禁止 Computer Use，必须缩小工作范围并保留相应边界。

## 1. 什么时候使用

适合使用本 skill 的任务包括：

- 从不完整 brief 推导建筑概念、功能组织、面积和流线；
- 为新建筑方案研究真实案例、比较策略并形成设计决策；
- 在 SketchUp Desktop 中创建、检查、修改、保存和导出可编辑模型；
- 继续修改已有建筑模型，同时保留用户的手工修改、场地内容和已确认决策；
- 检查平面、剖面、空间关系、门窗洞口、楼梯/坡道/电梯、结构概念、家具和场地关系。

下列任务不应自动扩大为完整建筑设计：

- 只收集官方 SketchUp 资料；这类任务应只保留原始资料和 provenance，不创建下游 skill 或模型；
- 已经解决方案的机械导出、尺寸微调或单纯 API 排错；这些任务通常不需要重新研究案例；
- 明确要求只做离线准备、禁止启动 SketchUp 或禁止 Computer Use 的任务；只能交付设计、脚本和静态检查结果。

当 brief 已经足够明确时，智能体应自主推进，不反复询问过程性问题。只有缺少的信息会阻止安全、有用或不可逆的下一步时，才提出问题。

## 2. 默认工作顺序

### 2.1 先建立设计基础

把输入拆成四类，并在项目记录中保留：

- **已给定**：用户明确的面积、场地、楼层、功能、保留对象、交付格式；
- **假设**：为了继续工作而暂定的朝向、气候、地形、面积口径、容量或材料；
- **推导**：根据活动、家具、人数、通行和运营关系计算出来的尺寸与组织；
- **未解决**：需要现场资料、规范、结构工程或用户决定才能确认的事项。

面积必须说明是 site area、footprint、GFA、net usable area 还是其他口径。不能把一个模糊的面积数字同时当作建筑面积和净使用面积。单位、楼层标高、场地边界、北向和交付阶段也要明确记录。

### 2.2 新方案先研究案例

新方案或需要建筑判断的重大修改，默认在定下平面、剖面和体量前研究若干真实建成项目。研究应围绕本项目的问题，而不是收集漂亮图片：例如公共/安静流线、窄地块服务、坡地无障碍入口、大跨度公共空间、保留结构或湿热气候遮阳。

每个重要案例至少记录：

- 项目名称、建筑师、地点、状态和阶段；
- 直接项目页面、图纸或负责方资料的 URL 与访问日期；
- 来源明确陈述的事实；
- 实际查看平面、剖面、照片或模型后观察到的内容及其局限；
- 智能体的解释、采用/改写/拒绝的原则，以及对本项目的适配原因。

不能把搜索摘要、转载图片或一个案例的宣传文字当作完整证据。不能把案例中的房间尺寸、屋顶形式、结构跨度、材料或性能直接搬到另一个场地。若用户明确要求 no-web、offline 或 supplied-sources-only，应尊重该要求并标记没有新研究的部分。

### 2.3 先设计，再建模

建模前至少解决到与任务阶段相称的程度：

- program、人数/容量、净面积和总面积账本；
- 公共、安静、后勤、服务和户外空间之间的邻接与冲突；
- 到达、入口、水平流线、竖向交通和服务路线；
- 楼层、剖面、结构网格、跨度、支撑连续性和屋顶逻辑；
- 门窗洞口、采光、隐私、遮阳、材料和立面节奏；
- 场地入口、保留对象、地形高差、排水、庭院、道路和公共界面。

选择体量时要说明它解决了什么问题。不要从固定的“住宅/展馆/社区中心生成器”开始，也不要因为案例外观漂亮就套用其形式。建筑应包含真实空间关系；一个只有墙皮和屋顶的装饰体块不算完成的建筑方案。

## 3. SketchUp 与 Ruby 运行要求

### 3.1 运行环境

- 精确几何和重复操作必须在 SketchUp 的主线程中运行；
- 系统 Ruby、下载的 API stubs 和 Python 脚本只能用于语法、文档、数据和离线检查，不能模拟 SketchUp 几何引擎；
- live 操作前检查 `Sketchup.version`、嵌入式 `RUBY_VERSION`、平台、活动模型路径/GUID、`active_path`、选择集、锁定状态、tags、材质、组件定义和 scenes；
- 每次操作都要确认当前模型是用户授权的目标模型，不能因为窗口名称相似就直接执行；
- API 细节优先查本地官方资料库和实际运行能力，不能把 stub 方法当成当前安装版本一定可用。

### 3.2 几何和组织

- JSON 和项目账本使用米、平方米等明确单位；Ruby 边界使用 `m`、`mm` 或 `inch`，只转换一次；
- 使用命名 group/component 和稳定的 semantic ID，使后续修改能定位到局部元素；
- 真正重复的窗、柱、结构 bay 使用 components；独立编辑的组合使用 groups；修改共享实例前先确认是否需要 `make_unique`；
- 原始边和面保持在 Untagged，tags 用于 group/component 可见性；不要把原始几何直接放进用户当前 tag；
- 墙体开口必须有真实的墙体中断、门窗框、洞口深度和必要的过梁/窗台关系；在墙前放一块玻璃不算开洞；
- 楼梯、坡道和电梯必须在剖面中形成真实可行的连接，不能只摆放一个楼梯形状；
- 家具、设备、服务空间和场地关系应按任务阶段建到足以检查使用方式的程度。

SketchUp 页面相机的兼容约定是：先设置 `active_view.camera`，再调用 `page.update` 保存场景状态；不要假设 `page.camera=` 可用。任何版本差异都应先探测并记录。

### 3.3 DWG 与外部输入

如果提供多个 DWG 或参考文件，先区分它们的角色：哪些用于最终建模，哪些只用于场地/尺度/分析。不能因为成功导入就假定单位正确。至少用一个已知长度或面积检查导入后的 SketchUp 尺度；毫米坐标被当成英寸等情况可能造成 1000 倍误差。

## 4. 证据等级和报告用语

必须把以下状态分开写，不能用较弱证据替代较强结论：

| 标签 | 可以支持的结论 |
| --- | --- |
| `OFFLINE CHECKED` | Python/Ruby 语法、数据合同、链接和离线 helper 检查通过 |
| `READY FOR LIVE TEST` | 静态材料足以进入原生 SketchUp 测试；还没有证明几何或视觉正确 |
| `LIVE GEOMETRY CHECKED` | 在实际 SketchUp 版本中运行过，并依据模型数据检查了指定几何/数量 |
| `VISUAL QA CHECKED` | 实际查看了平面、剖面、外部和重要内部场景，并记录了范围 |
| `PERSISTENCE CHECKED` | 写出文件、重新打开或读取保存结果后，确认模型和场景仍然存在 |
| `EXPORT CHECKED` | 导出的文件存在，并检查了格式、大小、元数据和必要的视觉内容 |

通过 `offline_checks.py` 只能标记 `OFFLINE CHECKED`，最多说明 `READY FOR LIVE TEST`。不能把非空 `.skp`、Ruby 返回成功、`manifold?`、HTTP 200 或截图中的静止窗口写成完整 QA。报告中应说明检查对象、版本、路径、样本范围和未覆盖的部分。

## 5. 项目记录和交付

项目资料应放在项目输出目录，不要写入安装后的 skill 或官方 source library。根据任务阶段，优先保留：

- `design.md`：brief、假设、设计目标、取舍和已确认决策；
- `precedents.md`：案例来源、访问日期、证据、观察、解释、限制和迁移原则；
- `project.json`：项目 UUID、revision、单位、楼层、GFA/net ledger 和检查口径；
- task-specific Ruby/Python 文件：每次有明确目的、绝对路径和报告输出；
- `audit/`：模型 inventory、数量、警告、错误、版本和检查范围；
- 版本化 `.skp`、PDF、图片、动画或其他用户请求的导出物。

交付前至少报告：

1. 交付文件的绝对路径、revision、实际存在性和合理文件大小；
2. 使用过的案例来源及其对方案的具体影响；
3. 面积、楼层、流线、洞口、结构概念和场地检查的结果；
4. 哪些是给定事实、哪些是假设、哪些是模型测量、哪些仍未验证；
5. 是否实际启动 SketchUp、是否做过视觉 QA、是否保存重开、是否验证导出；
6. 剩余缺陷、需要用户决定的事项和不能安全自动化的部分。

不要声称 OneDrive 同步、建筑规范合规、结构安全、消防通过或无障碍合规，除非这些结论有独立、适用且明确的证据。

## 6. 文件、重试和失败恢复

- 开始修改前保留已有用户内容和未保存状态；不要为了得到空模型而关闭或丢弃无关文档；
- 保存使用新路径和版本号，检查 `model.save`/`save_copy` 的返回值、实际路径、文件存在性和大小；
- 失败后先读取控制台、报告和模型状态，提出一个有证据的假设，再改变方法；
- 同一个失败特征连续出现两次后停止盲目重试，转为最小复现、查确切 API/版本，或报告外部阻塞；
- 事务中断后分别检查几何、scenes、tags、styles 和 revision；不能因为页面还在就假设整个操作已回滚；
- 任何用户明确的 `analysis-only`、`no-launch`、`no-Computer-Use` 或 `offline` 边界都必须保留在最终报告中；
- 不把凭据、Cookie、私有路径、未授权的客户资料或大型原始 source corpus 提交到公开仓库。

## 7. 官方资料库与缓存

官方技术资料库必须放在 skill 外部。设置后可运行：

```sh
export SKETCHUP_SOURCE_ROOT=/path/to/sketchup-modeler-source
python3 scripts/source_library.py status
python3 scripts/source_library.py status --verify
```

索引是可重建的派生缓存，默认放在用户 cache 目录，不应提交。source snapshot、仓库 commit 和安装时版本观察只能说明当时的证据；新的 live 任务必须重新探测当前 SketchUp、网页可用性和资料状态。

## 8. 推荐的用户请求写法

新方案：

```text
使用 $sketchup-architect。请先研究与这个 brief 的核心问题相关的真实建筑案例，
记录来源和访问日期，然后形成设计方案并在当前 SketchUp 中创建可编辑模型。
请保存 design.md、precedents.md、project.json、审计报告和版本化 .skp。
完成后分别报告离线检查、原生几何检查、视觉 QA、保存重开和导出的状态。
```

仅离线：

```text
使用 $sketchup-architect，但本次仅做 offline/no-launch 准备，不启动 SketchUp，
不要使用 Computer Use。请生成或检查设计记录、Ruby 脚本和静态测试，并明确标记
READY FOR LIVE TEST，不要声称模型或视觉 QA 已完成。
```

已有模型修订：

```text
使用 $sketchup-architect 修改现有模型。先读取当前 project.json、revision、模型
inventory 和未解决缺陷，只改与本次目标有关的元素；保留其他手工内容，保存新的
revision，并重新检查受影响的平面、剖面、数量和场景。
```

